
{%- macro list(tags) -%}
<ul>
  {%- for tag in tags -%}
    {%- set tagged = ['ext-',tag] | join  -%}
    <li>{{ tagged }}
    <li>{{ collections.ext-T9 | length }}
    {%- if collections.ext-T9 | length > 0 -%}
      <li>{{ tag | capitalize }}+
      <ul>
        {%- for post in collections.ext-T9 -%}
          <li>
            <a href="{{post.url}}">{{ post.data.title }}</a>
            {% if post.data.subtitle %}
              ({{ post.data.subtitle }})
            {% endif %}
          </li>
        {%- endfor -%}
      </ul>
    {%- endif -%}
  {%- endfor -%}
</ul>
{%- endmacro -%}