---
title: Home
description: "Guides for Exponential Idle. Most of the resources used by the community on Discord and Reddit are linked here."
home: true
---

{% extends "default.njk" %}

{% block title %}
  <h2 hidden>Home</h2>
{% endblock %}

{% block main %}

  <p>This is a site for guides for Exponential Idle.

  <p>Here are almost all of the available resources that are used in the Discord and Reddit.
  This website contains the guides that were created by Snaeky and
  Baldy, the theory sim­u­lator (sim) by XLII, and calculators from Eaux, Baldy, Niedzielan, AfuroZamurai, and Milla. 
  If you have any questions, please ask us on the <a href="https://discord.gg/3FqMuzuf4t">Exponential Idle Discord</a>
  or <a href="https://www.reddit.com/r/ExponentialIdle/">Exponential Idle Reddit</a>.

  <p>Do you have any suggestions for how to improve these guides?
  <a href="https://docs.google.com/forms/d/e/1FAIpQLSdje1OWIgI1vqMry6U1E51bxh3VE4tLilTdRyqun1aVmP7pag/viewform">
    Let us know your feedback</a> and <a href="https://bit.ly/ex-guide-suggestion-responses"> check out our responses.<a/>

  <h3>Our guides</h3>
  <ul>
    {%- for guide in collections.guides -%}
      <li>
        <a href="{{guide.url}}">{{ guide.data.title }}</a>
        {% if guide.data.subtitle %}
          ({{ guide.data.subtitle }})
        {% endif %}
      </li>
    {%- endfor -%}
  </ul>

  <h3>Guide Extensions</h3>

  <ul>
    {%- for pretag in ['T9'] -%}
      {%- set tag = ['ext-',pretag] | join  -%}
      {%- if collections[tag] | length > 0 -%}
        <li>{{ pretag | capitalize }}+
        <ul>
          {%- for post in collections[tag] -%}
            <li>
              <a href="{{post.url}}">{{ post.data.title }}</a>
              {% if post.data.subtitle %}
                ({{ post.data.subtitle }})
              {% endif %}
            </li>
          {%- endfor -%}
        </ul>
      {%- endif -%}
    {%- endfor -%}
  </ul>

  <p>{{ collections | keys | dump(2) | safe }}</p>

  {%- set collect = collections | keys | dump(2) | safe -%}
  {%- set ignore = ['all', 'guides'] -%}
  <p>{{collect}}</p>
  <ul>
  {%- for rem in ignore -%}
    <li>{{rem}}</li>
    <ul><li>{{collect | replace(rem, '')}}</li></ul>
  {%- endfor -%}
  {%- for val in collect -%}
    <li>{{val}}</li>
    {%- (set spliced = spliced.push(val, spliced)) if val != '' -%}
    <ul><li>{{spliced}}</li></ul>
  {%- endfor -%}
  </ul>

  <h3>Other resources</h3>
  {% include "tools.njk" %}

{% endblock %}
